<!DOCTYPE html>
<html>
  <head>
    <% include ./partials/head.ejs %>
  </head>
  <body>
    <% include ./partials/header.ejs %>
    <div class="container">
      <div class="article-tiles">
        <% articles.forEach(function(article) { %>
          <%- include('./partials/article-tile', { article: article }) %>
        <% }); %>
      </div>
    </div>
  </body>
</html>
